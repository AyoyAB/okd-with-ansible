- name: Api-Proxy - Copy over  configuration
  template:
    src: ./templates/{{item}}
    dest: /etc/nginx/{{item}}
  with_items:
    - tcpconf.d/kubernetes.conf
  become: true

- name: Api-Proxy - Restart nginx
  service:
    name: nginx
    state: restarted
  become: true
