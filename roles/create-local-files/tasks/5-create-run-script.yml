- name: Log first host in infras group
  ansible.builtin.debug:
    var: groups['infras'][0]

- name: Create installation script
  ansible.builtin.template:
    src: templates/install.sh
    dest: "{{ openshift_files_directory }}/install.sh"
    mode: '0755'
